# 複製此檔為 .env 並填入實際值

# Google Sheets 試算表 ID（網址中 /d/ 後面那段）
SPREADSHEET_ID=你的試算表ID

# OAuth 憑證路徑（從 Google Cloud Console 下載的 JSON）
CREDENTIALS_PATH=credentials/credentials.json

# 授權後自動產生，不需手動設定
TOKEN_PATH=credentials/token.json
